#chat.chat(ng-controller="chat" ng-init="getMsgList()"  ng-cloak )
  .container
    {{ lang.hello }}
    .msgList
      ul
        li(ng-repeat="msg in msgList")
          span.name {{ msg.name }}
          span.msg {{ msg.text }}
    .nameUser
      input(type="text"
        ng-model="newMsg.name"
      )
    .newMsg
      input(type="text"
        ng-model="newMsg.text"
        ng-keyup="putMsg(newMsg,$event)"
      )